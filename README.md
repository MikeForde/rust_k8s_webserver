# simple_rust_webserver

referenced https://github.com/actix/examples/blob/master/basics/hello-world/

# OpenShift

devcontainer (incl. Dockerfile) + Dockerfile (for OpenShift) adjusted such that can be deployed with pre-compiled binary as an experiment in reducing compile time on OS.
